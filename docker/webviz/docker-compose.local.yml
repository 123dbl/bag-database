services:
    bagdb:
        image: bag-database:local
        build:
            context: ../../
            dockerfile: docker/build/Dockerfile
        ports:
            - "8081:8080"
    webviz:
        ports:
            - "8082:8080"
